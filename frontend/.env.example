VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=

# Unified public backend entry. In the final deployment this should be A's AWS
# API Gateway URL, protected by Cognito JWT.
VITE_API_URL=

# Optional override for member D query API debugging only.
# Leave this empty in the final AWS-only mode so query/subscription calls use VITE_API_URL.
VITE_QUERY_API_URL=
VITE_USE_MOCK_QUERY=false
